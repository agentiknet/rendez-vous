{
  "name": "rendez-vous",
  "version": "0.1.0",
  "private": true,
  "description": "A shared, persistent agent room several humans drive at once, from WhatsApp, email or a laptop.",
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "packageManager": "pnpm@10.4.1",
  "scripts": {
    "check-types": "tsc --noEmit",
    "test": "node --test 'test/**/*.test.ts'",
    "probe:daemon": "node scripts/probe-daemon.ts"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "typescript": "~5.9.0"
  },
  "dependencies": {
    "qrcode-generator": "^2.0.4"
  }
}
